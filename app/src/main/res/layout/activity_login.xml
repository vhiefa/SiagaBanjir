<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dip">

    <!-- Email Label -->
    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Email"
        android:singleLine="true"
        android:layout_marginBottom="5dip"/>

    <!-- Email input text -->
    <EditText             android:padding="5dp"         android:background="@drawable/rounded_edittext" android:id="@+id/txtEmail"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dip"/>

    <!-- Password Label -->
    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Password"
        android:layout_marginBottom="5dip"/>

    <!-- Password input text -->
    <EditText             android:padding="5dp"         android:background="@drawable/rounded_edittext" android:id="@+id/txtPassword"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"

        android:password="true"
        android:singleLine="true"/>
    <LinearLayout
        android:layout_width="fill_parent"
        android:gravity="right"
        android:layout_height="wrap_content">

    <Button
        android:background="@drawable/transparan"  android:id="@+id/btnLupaPwd"
        android:layout_width="wrap_content"
        android:gravity="right"
        android:textSize="12sp"
        android:layout_marginBottom="10dip"
        android:layout_height="wrap_content"
        android:text="@string/lupapwd"/>

        </LinearLayout>
    <!-- Login button -->
    <Button         android:background="@drawable/color_btn" style="@style/ButtonText" android:id="@+id/btnLogin"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Login"/>

    <LinearLayout
        android:layout_width="fill_parent"
        android:gravity="center_horizontal"
        android:layout_height="wrap_content">
    <Button         android:background="@drawable/transparan" android:id="@+id/btnCreateAccount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:gravity="top"
        android:text="@string/blmpunyakun"/>
    </LinearLayout>

</LinearLayout>